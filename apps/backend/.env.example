# Puerto del servidor
PORT=3000

# Ruta de la base de datos SQLite (puedes cambiarla a una ruta local si lo prefieres)
DATABASE_PATH=central-backend.sqlite

# Ruta donde se crearán los workspaces de Playwright
PLAYWRIGHT_WORKSPACES_PATH=../playwright-workspaces

# Nivel de logging (debug, info, warn, error)
LOG_LEVEL=debug

# Clave secreta JWT (para futuras integraciones de autenticación)
JWT_SECRET=your-secret-key

# API Key (para futuras integraciones de seguridad)
OPENAI_API_KEY=your-api-key

# Configuración de generación (para sobrescribir archivos existentes)
OVERRIDE_EXISTING=false

# Ruta al directorio de plantillas base (usado por el generador)
TEMPLATE_DIR=../e-commerce.playwright-testing-model